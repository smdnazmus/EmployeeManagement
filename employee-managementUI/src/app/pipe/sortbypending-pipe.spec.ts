import { SortbypendingPipe } from './sortbypending-pipe';

describe('SortbypendingPipe', () => {
  it('create an instance', () => {
    const pipe = new SortbypendingPipe();
    expect(pipe).toBeTruthy();
  });
});
