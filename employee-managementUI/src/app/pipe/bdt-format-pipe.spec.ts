import { BdtFormatPipe } from './bdt-format-pipe';

describe('BdtFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new BdtFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
